# smithery.yaml
version: 1
startCommand:
  command: npx
  args:
    - "-y"
    - "@tasopen/mcp-alphabanana"
  env:
    GEMINI_API_KEY:
      description: "Google Gemini API Key from Google AI Studio"
      required: true